import{describe,it,expect}from'vitest';import jwt from'jsonwebtoken';import{authenticate,authorize}from'./middleware.js';
describe('security middleware',()=>{it('accepts a valid signed session',()=>{process.env.JWT_SECRET='test-secret';const req={cookies:{token:jwt.sign({id:'u1',role:'ADMIN'},'test-secret')},headers:{}};const res={};const next=()=>{req.called=true};authenticate(req,res,next);expect(req.user.id).toBe('u1');expect(req.called).toBe(true)});it('blocks roles without permission',()=>{const req={user:{role:'MEMBER'}};const res={status(code){this.code=code;return this},json(body){this.body=body}};const next=()=>{};authorize('ADMIN')(req,res,next);expect(res.code).toBe(403)})});
