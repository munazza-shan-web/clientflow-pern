services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: clientflow
    ports: ["5432:5432"]
    volumes: [clientflow_data:/var/lib/postgresql/data]
volumes:
  clientflow_data:
